export const getStoredFavorites = jest.fn(() => {
  return { type: 'mockedAction', payload: 'mockedPayload' }
})

export const addFavorite = jest.fn(() => {
  return { type: 'mockedAction', payload: 'mockedPayload' }
})

export const removeFavorites = jest.fn(() => {
  return { type: 'mockedAction', payload: 'mockedPayload' }
})
